#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
 * main -as seen
 *
 * Return: return success
 */

int main(void)

{
	char hpla;

	for (hpla =  'z'; hpla >= 'a'; hpla--)
	{
	putchar(hpla);
	}

	putchar('\n');

	return (0);
}
