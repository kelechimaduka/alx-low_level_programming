#include <stdio.h>

/**
 * main - Entry point
 *
 * Description : using puts instead of printf
 *
 * Return: Returns zero
 */

int main(void)
{
	char str[] = "Programming is like building a multilingual puzzle, ";

	puts(str);

	return (0);
}
